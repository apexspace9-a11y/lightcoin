name: light_coin
description: Light Coin - lịch, tiết kiệm và nhắc hẹn cá nhân.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^22.2.0
  flutter_timezone: ^5.1.0
  intl: ^0.20.3
  shared_preferences: ^2.5.5
  table_calendar: ^3.2.0
  timezone: ^0.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
